<h1>User Details</h1>

<ul>
  <% @users.each do |user| %>
    <li>
      <%= link_to user.name, get_user_details_user_path(user)%>
    </li>
  <% end %>
</ul>

<%= link_to "New User", new_user_path %>